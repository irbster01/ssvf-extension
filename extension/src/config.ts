// Configuration for the extension
export const API_URL = 'https://ssvf-capture-api.azurewebsites.net/api/captures';

// API key removed for security - authentication now handled by backend session tokens
// Each user must authenticate through the extension popup before capturing data
